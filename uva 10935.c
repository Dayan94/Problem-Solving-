#include<stdio.h>
#include<stdbool.h>
int main()
{
    unsigned char n = 0, i = 0, left = 0;
    stack<int> cards;
    while(scanf("%hhu", &n) == 1)
    {
        left = n;
        throw_card = 0;
        while(left != n)
        {
            for(i = 1; i <= left; i++)
            {

            }
        }
    }
    return 0;
}
